# Decision log

One line per record. The files in this directory are the numbering authority; this index is regenerable.

- [001: The project stores data in PostgreSQL](001-database-engine.md) — seeded at setup, Stakes: high
- [002: Money gets property tests from `fast-check`, and nothing else is added](002-property-testing-for-money.md) — `fast-check@4.9.0`, devDependency-only, catalog-pinned; no `@fast-check/vitest`, Stakes: high
- [003: Every Delta on a line is measured against the item's own price, and only one can be a multiplier](003-delta-composition.md) — ratifies the built arithmetic (contributions summed against the base, order-independent, exact); adds a one-multiplier-per-OrderLine guard in `catalog` and `checkout` that keeps the rule free to reverse, Stakes: high
- [004: Kysely talks to PostgreSQL through `pg`, the same driver the sibling project runs](004-postgres-driver.md) — `pg@8.22.0` dependency + `@types/pg@8.20.3` devDependency in `packages/backend` only, no catalog pin, Kysely's built-in `PostgresDialect`; adopts ApxDenta's production Bun setup; transaction-affinity guaranteed by Kysely's driver, so area 2 uses `set_config(..., true)`; no `pg-native`, Stakes: high
- [005: The Prisma commands become root scripts, and the root `.env` reaches them](005-prisma-command-scope-and-env.md) — resolves the ADR-0004 / ADR-0008 / issue-03 contradiction: `prisma` stays in `packages/backend` only, and root `codegen`/`migrate`/`migrate:status` scripts scope into it with `vp exec -F backend`; the three `ORC2_*_CMD` values become `vp run -w <script>`; the migrate scripts source the root `.env` because Prisma never searches parent directories; a root `prepare` hook makes `vp install` alone reach a green gate. ADR-0008 and record 004 untouched; ADR-0004 amended, Stakes: high
- [006: The oRPC packages, and zod 4 without the zod integration package](006-rpc-and-validation-dependencies.md) — `@orpc/contract` + `@orpc/client` in `packages/contract`, `@orpc/server` + `hono@4.12.33` in `apps/api`, `@orpc/tanstack-query` in both front ends, all `@orpc/*` at `1.14.13` and catalog-pinned as a lockstep family; `zod@4.4.3` catalog-pinned in `schemas`/`contract`/`backend`; **no `@orpc/zod`** (it is an OpenAPI converter and DeanPOS publishes no OpenAPI document — removing it removes the only zod-major constraint) and **no `@hono/*` adapter** (oRPC's own `@orpc/server/fetch` covers it); all `dependencies`, none dev; client built once in `packages/contract` as a factory taking `fetch`, so the in-process `app.request()` seam has a single injection point; the missing-procedure compile error verified from oRPC's type source, not its docs, Stakes: high
- [007: The shared UI package — Tailwind 4's shared theme file, two vendored primitives, and a contrast test with no library behind it](007-shared-ui-dependency-set.md) — **React 19.2.8 named here, because no manifest declared one**; Tailwind 4 has no `presets` array, so "a preset both apps extend" becomes `packages/ui/src/theme.css` holding `@theme`, imported by two identical two-line app stylesheets (`@source` resolves relative to the importing CSS file, so the registration travels with it and neither app configures anything); shadcn vendored via `shadcn init --base radix` — **the flag is mandatory, Base UI is shadcn's default since July 2026** — yielding `radix-ui@1.6.7` + `class-variance-authority@0.7.1` + `clsx@2.1.1` + `tailwind-merge@3.6.0` + `lucide-react@1.28.0` in `packages/ui`'s `dependencies` and `shadcn@4.16.1` dev, with `@radix-ui/react-slot@1.3.3` + `@radix-ui/react-dialog@1.1.23` as the written-out fallback; catalog-pinned: `react`/`react-dom@19.2.8`, `@types/react@19.2.18`, `@types/react-dom@19.2.4`, `tailwindcss`+`@tailwindcss/vite@4.3.3` (a lockstep pair), `lucide-react`; **not** pinned: the five single-declarer packages, per record 004's `pg` precedent; the minimum primitive set is **`button` and `sheet` only** — `sheet` because the 390 back-office mock draws the nav behind a `☰` — with `alert`, `separator`, `collapsible`, `dropdown-menu`, `card`, `table`, `input`, `skeleton` and `toast` each excluded with a stated reason; the contrast test is **hand-written from the W3C formula with no dependency**, reading `theme.css` as the single source, which is why colour tokens must be six-digit sRGB hex and never OKLCH, and it fails if any `--color-*` token appears in no pairing; touch targets are two tokens with W3C-normative values — `--min-target-size: 24px` (SC 2.5.8, AA) repo-wide and `--min-touch-size: 44px` (SC 2.5.5, AAA) for `apps/pos`, in `px` because the criterion is stated in CSS pixels; the focus indicator is a `@layer base` `:focus-visible` rule in the same shared file, adopting SC 2.4.13's AAA figures because AA's "visible" is not testable; grants a narrow exemption from code standard 2 for generated files under `packages/ui/src/components/`; `apps/landing` deliberately outside `packages/ui` until area 11, which reuses the same `theme.css` through `@tailwindcss/postcss`; **the Tailwind major is the one part that is not affordably reversible** and carries a named re-check trigger, Stakes: high
- [008: The front-end application dependency set — a generated route tree that is not committed, and one render seam that lives beside the server one](008-frontend-application-dependency-set.md) — nine packages across three workspaces; catalog-pinned `@tanstack/react-router@1.170.18` + `@tanstack/router-plugin@1.168.23` + `@tanstack/router-cli@1.167.21` (**a lockstep family** — plugin and CLI share `@tanstack/router-generator@1.167.21`, so a partial bump makes the two generation paths write different trees, invisibly, because the file is gitignored), `@tanstack/react-query@5.101.4`, `@vitejs/plugin-react@6.0.5` (**not** the deprecated `@vitejs/plugin-react-oxc`), `happy-dom@20.11.1`; **not** pinned, one declarer each per record 004's `pg` precedent: `@testing-library/react@16.3.2`, `@testing-library/dom@10.4.1` (a *peer* of RTL 16, so it must be declared), `axe-core@4.12.1`; the route tree is generated into `apps/<app>/src/generated/routeTree.gen.ts` — **gitignored by the existing `**/generated/**` rule, because the PRD already says generated output lives there and a route tree is an ordered index file, the merge case ORCHESTRATOR.md names as dangerous** — regenerated by `tsr generate` folded into record 005's root `codegen` script, so `prepare` covers a clean clone with no new mechanism; `ORC2_GATE` gains `vp run -w codegen;` at the front, which is what makes "a link to a removed route fails the build" a property rather than a claim (the plugin regenerates on `configResolved`, i.e. at Vitest startup, which runs *after* both typecheck steps); the property is additionally pinned by a committed `@ts-expect-error` fixture copying issue 04's shape; **the render half of the one seam lives at `apps/api/src/test-seam-react.tsx` beside the server half** — ADR-0009 rule 6 forbids the apps importing each other and an eleventh workspace would contradict the PRD's ten — re-exporting `screen`/`renderRoute`/`expectNoAxeViolations` so neither app declares Testing Library or axe; record 006's devDependency boundary becomes an executable `assertNoServerImports(srcDir)` test rather than a review-time grep; axe runs `runOnly` scoped to the five WCAG tag strings with **exactly `color-contrast` disabled** and `link-in-text-block` pre-authorised as the only permitted extension; no `@testing-library/jest-dom`, no `user-event`, no devtools, no `@tanstack/react-router-with-query`, no second Vitest; `environment: "happy-dom"` set per-app because `environmentMatchGlobs` was removed in Vitest 4, and RTL auto-cleanup does **not** fire without global `afterEach`, so the helper registers it, Stakes: high
- [009: The terminal shell's chrome — what renders when there is no tenant, no cashier, no network, and no data yet](009-terminal-shell-chrome-states.md) — answers screen-triage question 3 for issue 06, which issue 07 consumes unchanged; one principle applied six times: **render what is true, render nothing where nothing is true, and never let a state be a shape nobody drew**; the mock's three top-bar slots become one — the start slot renders the literal `DeanPOS` and the `OFFLINE · 3 queued` and `Ana (cashier) · Lock` slots render **no element at all**, not a dimmed placeholder and not an empty reserved box, with `justify-between` carrying the mock's order so areas 2 and 5 add a child and move nothing; focus is record 007's rule **confirmed, not re-decided**, plus three constraints it does not give (DOM-order focus, no `overflow-hidden` clipping the 2px-at-2px-offset ring, no per-component restyling); hover exists only on `packages/ui`'s `Button` and **nothing in `apps/pos` may be reachable only by hover** (it is a touch device); `Try again` is **never disabled** — TanStack Query dedupes in-flight fetches, and a disabled button leaves the tab order at the worst moment; loading is **one line of `<p role="status">Loading…</p>`, never a skeleton or spinner** (the README lists them under "Not drawn, on purpose" and a skeleton is a shape nobody drew), with the header and frame painted on first paint; **there is no empty state — a ping that returns no row is an error, not "nothing here yet"**, since the row is a migration invariant (standing rule: an empty list is an empty state, a missing singleton is an error); the error is a `role="alert"` block with two plain sentences and a retry, in `--color-foreground` on `--color-background` because `--color-destructive` on `--color-background` is **not in record 007's asserted pairing table**, carrying no status code, no URL and no server text; the two layouts are a **CSS `md:` media query, not JavaScript and not a container query**, with regions that have no content rendering no element; plus `lang`, `<title>`, a viewport meta with **no scale lock**, one `<header>`/one `<main id="main-content">`, no skip link in `apps/pos` (nothing repeats) and one in `apps/backoffice` (the sidebar does), and no motion anywhere in the chrome; the four sentences of copy are replaceable without a new record, the roles/region/retry/no-technical-detail rules are not; least-confident value is the 768px `md` boundary, which is a Tailwind default and carries a re-check trigger, Stakes: medium
- [010: The word "layout" in the routes layer — ADR-0009 means nesting, not markup, so no route file contains JSX](010-the-word-layout-in-the-routes-layer.md) — resolves the ADR-0009 / code-standard-4 contradiction in favour of the standard's reading: **"layout" in the routes layer means layout *nesting* — which screens sit inside which shell — and never layout markup**; decided on the ADR's own second use of the word (`(auth)/  route groups for layout, not for URLs`, and a parenthesised route group is "purely organizational", so it cannot mean markup) and on the codebase the ADR is adapted from, where `__root.tsx` renders `<Outlet />` and nothing else, the one pathless layout route is a guard plus `component: Protected`, and the shell's JSX lives in `features/protected/Protected.tsx`; the resulting rule is **zero-exception, the root included — a route file contains no JSX**, every `component`/`pendingComponent`/`errorComponent`/`notFoundComponent` is a bare imported identifier, and a layout route that only guards or only groups **omits `component` entirely** because TanStack Router then renders an `<Outlet />` automatically (the documented behaviour that makes a bright line affordable, and the sentence to re-check at any router major bump); rule 4's intent is *strengthened* — "how much layout is too much" was taste, "is there JSX in this file" is a read; the reviewer's test is `rg -n '</|/>' apps/*/src/routes` returning nothing (a TS generic produces neither `</` nor `/>`), scoped to `src/routes/` so `src/router.tsx`'s one-line `defaultErrorComponent` adapter is **not** a breach, with chrome hidden under a `-` prefixed folder inside `routes/` closed off explicitly; the shell frame goes to `apps/<app>/src/components/AppShell.tsx` **not** `features/shell/` (it owns no data, and `features/<area>/` mirrors PRD areas) — the runner-up by 6 points, and the right answer the day the shell gains a query; **the issue-06 lane does change, against the reviewer's ruling**: `routes/__root.tsx` splits into itself plus `components/AppShell.tsx`, identical output, no test and no manifest touched, because "no feature to delegate to" is false in a lane whose `ErrorState` and `NotFoundState` already sit in `components/` and because issue 06 calls that file the worked example ten areas copy; ADR-0009 amended in place plus an amendment paragraph in record 005's style, code standard 4 replaced wholesale, **record 009 amended and not overturned** — only the file path in its reversal section moves, none of its decisions change; reversal cost is bounded by the number of *shells*, not routes (`rg -l 'AppShell' apps`), Stakes: medium
- [011: The local stack and the versioned deploy — Caddy, one proxy that also serves the two bundles, and a git SHA as the image tag](011-local-stack-and-versioned-deploy.md) — **Caddy `2.11.4-alpine`** wins on the one constraint that separates the candidates: it is the only one of the three with a **certificate authority of its own**, so four origins get real TLS on a laptop with no DNS, no ACME and no credentials — and that is a *correctness* requirement, not comfort, because `apps/api/src/middlewares/cors.ts` already ships `https://pos.${appDomain}` hard-coded and tested, so a plain-`http` local stack fails CORS against merged code; **there is no local mode and no production mode** — Caddy picks the issuer from the hostname (internal CA for `.localhost`, Let's Encrypt HTTP-01 otherwise), so the `APP_DOMAIN` variable the PRD already required is the whole switch: one Caddyfile, no override file, no template step; **four enumerated site blocks, never a wildcard** — a wildcard forces DNS-01, a provider plugin and an API token, so enumerating removes a credential from the deployment outright; **the proxy serves both React bundles itself from `/srv/pos` and `/srv/admin` — no static containers** (origin isolation is a property of the *hostname*, not the process, so ADR-0007 is untouched and the reviewer's obvious objection is answered in the record), with `try_files {path} /index.html` because without it every SPA deep link 404s on refresh; **four Compose services** (`postgres`, `api`, `landing`, `web`) and **three volumes** — `caddy_data` is not optional, losing it means a rotated local CA or a Let's Encrypt rate-limit lockout; the `api` service's `DATABASE_URI` is **derived from the `POSTGRES_*` parts, not copied from `.env`**, because the host's value points at `localhost` for the gate and a container using it would dial itself; **nothing sits between the API and PostgreSQL** — record 004's statement-mode-pooler no-go restated as a property of the stack; **Next.js `16.2.12`** in `apps/landing`, un-catalog-pinned per record 004's single-declarer precedent, with `react`/`react-dom` at `catalog:` so **the catalog constrains Next, not the reverse** (peer `^19.0.0`, satisfied by 19.2.8; Next does not vendor the app's React), `output: "standalone"`, **`next-env.d.ts` committed** because a clean clone typechecks before anything has built — the record-005 trap a third time — and `apps/landing` still outside `packages/ui` and Tailwind until area 11 (record 007 confirmed, not re-decided); base images `oven/bun:1.3.13-slim` (**tied to `devEngines.packageManager` — they move together or not at all**), `caddy:2.11.4-alpine`, and **`node:24.13.0-slim` for the landing runtime** — the one deliberate second base image, because Bun-as-Next-runtime is first-party to Bun and Vercel but not to Next's own deployment docs, and area 11 inherits whatever lands; **the image tag is `git rev-parse --short=12 HEAD`**, the same SHA on all three images so one variable rolls the whole stack back — `IMAGE_TAG=<sha> docker compose up -d`, no rebuild and no code revert — with **no `latest`**, `org.opencontainers.image.revision` on each image, and the honest limitation that with no registry decided (credentials are outside the mandate) `docker image prune -a` destroys the rollback targets; the carried-forward clean-clone gap from issue 03 closes as **one command, `vp run -w stack`**, which writes `.env` (values `docker-compose.yml` already carries as its own fallbacks, so `.env.example` still holds none), runs `vp install` because "from a clean checkout" means there is no `node_modules`, `docker compose up -d --wait`, then `vp run -w migrate` — **written in one `cat >` and never copy-then-append**, because `vitest.setup.ts` keeps the *first* value for a key and an empty `DATABASE_URI=` would beat the real one; the database test is **not** made to skip itself; `.env.example` gains exactly **one name, `IMAGE_TAG`**; **eight of the nine acceptance criteria are fully completable and verifiable with no VPS — including the rollback one** — and the ninth splits, its *documentation* half writable and reviewable now and only its *execution* deferred to a three-item human list (four DNS records, confirm public ACME issuance, run the smoke check for `{"live":true,"databaseReachable":true}`); weakest link named honestly: **no primary source confirms Caddy's internal issuer covers multi-label `.localhost` names**, which is why evidence scores 4 and why the fallback (`tls {$CADDY_TLS:internal}`) is pre-decided — and deliberately *not* chosen up front, because it fails in *production* if someone forgets the variable whereas the chosen shape fails on the developer's own laptop; flags one conflict rather than overriding it silently — record 008's "devDependencies are not installed in a production image" is untrue of this image, its conclusion survives on Bun's import-graph laziness, and it is **amended in passing, not overturned**, Stakes: high
- [012: Development front ends are admitted by an entry point the production image never runs](012-development-origins-and-the-dev-server.md) — answers issue 09's open question: **`apps/api/src/middlewares/cors.ts` and `apps/api/src/index.ts` both get a zero-line diff**, because the two development origins are passed as literals from a *second* entry point, `apps/api/src/dev.ts`, that `docker/api.Dockerfile`'s CMD never runs — so there is no setting to fill in wrongly and no "is this development?" condition to write backwards, and widening the live API requires editing one of two files a reviewer reads; `createApp` gains one optional `devOrigins?: string[]` defaulting to `[]`, which is the entire mechanism; **issue 04's CORS test passes unchanged and becomes environment-independent** — `createTestSeam()` passes no `devOrigins`, so the test now measures the production allowlist regardless of anything in anyone's shell — with `test-seam.ts` gaining a passthrough and one new `cors-dev-origins.test.ts` proving the parameter is *additive and explicit* (a passed `http://localhost:5173` is echoed, `https://attacker.example.com` still gets no header), so nothing here needs `vp run dev` to be verified; ports pinned **api 3000 / landing 3001 / pos 5173 / backoffice 5174**, with **`strictPort: true` as part of the security answer, not tidiness** — Vite's documented default silently moves to the next free port, and a silently-moved front end gets refused by CORS, whose tempting "fix" is widening the allowlist; `localhost` only, never `127.0.0.1` (different origins to a browser), and `apps/landing` deliberately absent, mirroring the PRD's apex exclusion; **`VITE_API_URL` holds the origin only — scheme, host, port, no path and no trailing slash** — because `/rpc` is a fact about `app.use("/rpc/*")` and not about the environment, giving `${import.meta.env.VITE_API_URL}/rpc` in both `lib/orpc.ts` files, `http://localhost:3000` in development from the root `.env`, and `https://api.${APP_DOMAIN}` in production baked at image build time by a new `ARG`/`ENV` pair in `docker/web.Dockerfile` plus an `APP_DOMAIN` build arg in compose — **which closes a live defect: nothing sets `VITE_APP_DOMAIN` at build time today, so the shipped bundles call `https://api.undefined/rpc`**; `envDir` deliberately left at its default so a stray root `.env` can never leak into a bundle, both environments using the single mechanism of a shell variable already present when Vite runs; **the gate stays green with the variable unset and no manual step is added** — verified in the installed `@orpc/client@1.14.13` source, `RPCLink`'s constructor stores `url` without constructing a `URL`, so an unusable value is inert until a call the front-end tests never make, and an import-time guard is therefore explicitly forbidden; root `"dev": "set -a; [ -f .env ] && . ./.env; set +a; vp run -r --parallel dev"` copies record 005's `migrate` prefix character-for-character rather than inventing a mechanism, which is what reaches `apps/api` with `DATABASE_URI` (Bun's autoload is cwd-only) and both Vite servers with `VITE_API_URL`; `apps/api` needs a `dev` script distinct from the container's precisely because the divergence *is* the security property (`bun run --hot src/dev.ts` vs `bun run apps/api/src/index.ts`), `--hot` per Hono's own Bun docs; **the premise that Fashio only runs watch tasks is wrong** — it runs three long-running servers under exactly this command, so four need no special handling, Vite+ documents dev servers as excluded from `run.cache`, and stopping is Ctrl-C with a documented `lsof -ti:3000,3001,5173,5174 | xargs kill` fallback because **no primary source confirms how `vp run --parallel` forwards SIGINT**; second unverified point carries a pre-decided fallback too (explicit `-F` filters, if `-r` errors on the six packages that have no `dev`); the runner-up by three points is a `NODE_ENV === "development"` check inside `cors.ts` — safe polarity, already pinned to `production` by the Dockerfile — and it loses only because the widening code would then exist inside the running production process; **the reviewer's test is eight greps and three empty diffs**, of which check 8 (`docker/api.Dockerfile` still runs `index.ts`) is the one that would silently void the record; named re-check trigger is LAN-device testing of `apps/pos`, whose successor is pre-decided as an environment variable read **inside `dev.ts` only**, Stakes: high
- [013: Two densities from one attribute on `<html>`, and `accent` stays shadcn's word](013-density-mechanism-and-token-names.md) — answers both of issue 11's stated questions; **density is `<html data-density="touch"|"compact">` and nothing else**, working because a Tailwind 4 utility contains a `var()` reference rather than a value (`p-4` emits `padding: calc(var(--spacing) * 4)`, quoted from Tailwind's padding docs), so re-declaring `--spacing` on an ancestor rescales every spacing utility beneath it with no second class and no component edit — compact is Tailwind's own default and puts the reference's 8px grid on the even steps, touch is that scale **× 1.25**, a factor *derived* rather than chosen because 4px→5px is the smallest whole-pixel rescale that lifts stock shadcn `h-9` from 36px to **45px**, over record 007's 44px floor, without touching a component; the override block sits in `@layer base` because Tailwind's entry declares `@layer theme, base, components, utilities;` and `@theme` output lands in `theme`, so `base` wins deterministically rather than by source order, and only the `--text-*` font sizes are overridden because Tailwind's default line heights are **unitless ratios** (`calc(1.25 / 0.875)`) that scale themselves; **the attribute must be on `<html>`, never a shell `<div>`** — `sheet`, `sidebar`'s drawer and `select`'s popper are Radix portals that mount into `document.body`, so a shell-scoped attribute silently reverts every portalled surface in `apps/pos` to compact; **`target-min` and `touch-min` collapse into one utility `tap-target`** reading a new `--tap-size` that is `var(--min-target-size)` at base and `var(--min-touch-size)` under touch — record 007's two W3C-provenance tokens are untouched, they stop being classes and become the two values a third token selects between, because a shared component rendered by both apps has no way to pick — **records 007 and 009 amended in passing, not overturned** (four live call sites today); the app-root contract is four clauses, of which clause 3 is **no component in `packages/ui` reads the attribute, branches on it, or takes a `density` prop**, and one five-line `index.html` assertion per app in issue 15 exists because **nothing else in the repository would notice `apps/pos` rendering compact** (happy-dom computes no layout, axe's `color-contrast` is disabled in a virtual DOM, the contrast test reads only `--color-*`); on the names, **`accent` keeps shadcn's meaning — hover surface, a neutral, never a hue — and ADR-0013's "accent" becomes the `status-` prefix**, so **every already-generated component gets a zero-line diff** and no future `shadcn add` needs its `hover:bg-accent hover:text-accent-foreground` rewritten (the runner-up loses on engineering cost 1: renaming would mean hand-editing generated output forever, which destroys issue 13's vanilla-baseline premise on the first pull); the set is **exactly 35 `--color-*` tokens** — 19 shadcn neutrals/surfaces including `popover*` for `select` and `destructive-foreground` (which upstream has dropped but the vendored `button.tsx` still uses), all 8 `sidebar*` names, and 8 status tokens over four roles `success`/`warning`/`info`/`danger` each as `-tint` and `-tone` — with **38 pairings**, and the `-tint`/`-tone` split *is* the "never under text" rule made enforceable: a `-tone` has no `-foreground` partner and appears as a background in no pairing, so there is nothing to put a label on, while `foreground` on each `-tint` at 4.5:1 is what makes issue 14's badge label legal; **no `--color-chart-*`** (the status tones are the chart series; ADR-0013 says so, and a second vocabulary for the same four hues is the drift `packages/ui` exists to prevent) with a named trigger at area 7's first five-series report; **no DeanPOS-prefixed vocabulary behind `@theme inline`** because the contrast test's regex cannot see through indirection — green suite, unchecked palette, which is the block record 007 already deleted once; radius is **density-invariant** and needs no new names; `--font-sans` not `--font-manrope`, because Tailwind's `--default-font-family: --theme(--font-sans, initial)` is what actually sets the body face; tabular figures **global on `:root`**, not a class on money columns; **a finding issue 11 needs before it picks values: `#35CCA6` is 2.03:1 on white, so it cannot be `status-success-tone`** — the tones are darker siblings, the pattern ADR-0013 already used for destructive; flags for issue 13 rather than deciding them: shadcn's radix registry has **moved to `cn-*` component classes** backed by a stylesheet (the vendored `button.tsx`/`sheet.tsx` predate it), and the injected sidebar CSS may reference bare `var(--sidebar-*)` which this record deliberately does not re-alias; token names are **free to reverse today and only today** — no screen consumes any of the 35 — and the weakest point is stated plainly: 1.25 is derived from the 44px floor but nobody has seen a Card, Badge and Table at it, **re-check trigger issue 15**, fix costs one number, Stakes: high
