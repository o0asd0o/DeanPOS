# Discount capture carry-forward

Renaming a Discount's `requiresReference` label must not alter references already captured on an Order. Once the Order entity exists, assert capture-at-sale for the Discount type, value, name, and reference (Issue 06, Scenarios row 23; deferred from catalog).
