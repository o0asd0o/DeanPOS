# Values are sourced by a shell in the root migrate scripts — quote any value
# containing spaces, & or ?.
# DATABASE_URI: the migration owner. Only the root migrate/migrate:status
# scripts and prisma.config.ts read it.
DATABASE_URI=
# APP_DATABASE_URI: the running app and the test seam connect as this role
# instead — never superuser, never the owner of a tenant-owned table. The
# role and its (fixed, non-secret — RLS FORCED is the real boundary) password
# are provisioned by packages/backend/src/db/prisma/migrations/
# 20260802065946_tenant_isolation_spine/migration.sql, which both a lane and
# a deployment run. Issue 01, tenant-isolation-spine.
APP_DATABASE_URI=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# The registrable domain apps/api derives its CORS allowlist from —
# https://pos.<APP_DOMAIN> and https://admin.<APP_DOMAIN> (ADR-0001, PRD
# "The CORS allowlist is two of the four origins"). Not decided; the
# four-origin shape is. Locally this is deanpos.localhost, which resolves to
# loopback and gets Caddy's internal certificate authority with no DNS.
APP_DOMAIN=

# The container image tag every service is deployed at — a git commit SHA,
# `git rev-parse --short=12 HEAD`. docker-compose.yml defaults it to `dev`, so
# a local `docker compose up --build` needs nothing set. Rolling back is
# setting this to a previous SHA and running `docker compose up -d`.
IMAGE_TAG=

# The API origin apps/pos and apps/backoffice call — scheme, host, port, no path.
# Development: http://localhost:6001. Production doesn't read this file;
# docker/web.Dockerfile bakes https://api.${APP_DOMAIN} in at build time.
VITE_API_URL=
